/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['aceternity.com', 'assets.aceternity.com', 'lh3.googleusercontent.com', 'via.placeholder.com', 'images.unsplash.com', 'flagcdn.com', 'png.pngtree.com', 'res.cloudinary.com'], // Add the external domain here
    },
};

export default nextConfig;
